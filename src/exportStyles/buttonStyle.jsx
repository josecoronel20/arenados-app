import React from "react";

const buttonStyle = (bgColor,textColor) =>{ return `p-1 rounded ${bgColor} ${textColor} hover:scale-105 duration-200 cursor-pointer active:scale-95`}

export default buttonStyle;