import React from "react";
import Month from "../components/Home/Month";

const Home = () => {
    return(
        <main className="bg-darkGrey w-screen overflow-hidden min-h-screen">
        <Month />
        </main>
    )
};

export default Home